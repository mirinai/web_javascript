//화살표 함수[arrow function]

// const plus100 = (x) => {
//   return x + 100;
// };

// const plus100_ = (x) => x + 100; //한줄이면

// //1. 원소 3번째 돌려주기
// //2. 소문자로 만들고 이모지 붙이기

// returnThird = (x) => {
//   return x[2];
// };
// returnThird_ = (x) => x[2];

// toSmall = (x) => {
//   return x.toLowerCase + "이모지";
// };
// toSmall_ = (x) => x.toLowerCase + "이모지";

// returnThird(["a", "b", "c"]); //c
// toSmall("APPLE"); //apple이모지
